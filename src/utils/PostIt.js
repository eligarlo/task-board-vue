export default class PostIt {
  constructor({ id, title, description, color, date }) {
    this.id = id,
    this.title = title, 
    this.description = description,
    this.color = color,
    this.date = date
  }
}
